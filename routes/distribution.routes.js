// routes/distribution.routes.js
// GET  /api/distributions            — recent releases
// POST /api/distributions            — release a claim (validate code + log)
// GET  /api/distributions/validate/:claimCode  — check if code is valid
